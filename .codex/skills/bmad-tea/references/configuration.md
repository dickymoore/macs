# TEA Configuration (summary from docs)

- TEA config lives in `_bmad/bmm/config.yaml` (installer-generated).
- Canonical schema lives in the BMAD repo `src/bmm/module.yaml` (reference only).
- Common options include Playwright utils and MCP enhancements flags.
